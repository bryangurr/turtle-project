<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #004d40, #66bb6a);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .form-box {
            background: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 400px;
            max-width: 100%;
            text-align: center;
        }

        .form-box h1 {
            margin-bottom: 20px;
            color: #004d40;
        }

        .form-box label {
            display: block;
            text-align: left;
            margin-bottom: 5px;
            color: #333;
        }

        .form-box input[type="text"],
        .form-box input[type="password"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        .button-container {
            display: flex;
            justify-content: space-between;
            gap: 10px;
        }

        .form-box button {
            background-color: #66bb6a;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .form-box button:hover {
            background-color: #004d40;
        }

        .btn.delete {
            background-color: #e57373;
        }

        .btn.delete:hover {
            background-color: #c62828;
        }
    </style>
</head>

<body>
    <div class="form-box">
        <h1>Edit Admin</h1>
        <form action="/edit_employee/<%= admin.id %>" method="POST">
            <input type="hidden" name="id" value="<%= admin.id %>">
            <input type="hidden" name="old_email" value="<%= admin.username %>">
            <input type="hidden" name="old_password" value="<%= admin.password %>">
            <label for="first_name">First Name</label>
            <input type="text" id="first_name" name="first_name" value="<%= admin.first_name %>" required>
            <label for="last_name">Last Name</label>
            <input type="text" id="last_name" name="last_name" value="<%= admin.last_name %>" required>
            <label for="username">Username</label>
            <input type="text" id="username" name="username" value="<%= admin.username %>" required>
            <label for="password">Password</label>
            <input type="password" id="text" name="password" value="<%= admin.password %>">
            <label for="phone">Phone</label>
            <input type="text" id="phone" name="phone" value="<%= admin.phone %>" required>

            <!-- Buttons are now in a container -->
            <div class="button-container">
                <button type="submit">Submit</button>
        </form>
                <form action="/delete_employee/<%= admin.id %>" method="POST" style="margin: 0;">
                    <button class="btn delete" type="submit">Delete</button>
                </form>
            </div>
    </div>
</body>

</html>
