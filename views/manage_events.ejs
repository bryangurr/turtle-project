<!DOCTYPE html>
<html>
<head>
  <title>Manage Events</title>
  <style>
    body {
    font-family: Futura, sans-serif;
    margin: 0;
    background-color: #f5f5f5;
    padding: 20px;
}

h1 {
    color: #004d40;
    text-align: center;
    margin-bottom: 15px;
}

.header-buttons {
    display: flex;
    justify-content: flex-start;
    margin-bottom: 15px;
}

.btn {
    padding: 8px 15px;
    font-size: 12px;
    color: white;
    background-color: #66bb6a;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    text-decoration: none;
    margin-right: 10px;
    transition: background-color 0.3s ease;
}

.btn:hover {
    background-color: #004d40;
}

.table-container {
    margin: 0 auto;
    width: 100%;
    background-color: #ffffff;
    padding: 15px;
    border-radius: 8px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    overflow-x: auto;
}

.table-container table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 15px;
    background-color: #ffffff;
    border: 1px solid #e0e0e0;
}

th, td {
    padding: 8px 16px;  /* Reduced padding */
    text-align: left;
    border-bottom: 1px solid #e0e0e0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 150px;
    line-height: 1.3;  /* Tighter line height */
}

th {
    background-color: #66bb6a;
    color: white;
    font-size: 13px;
    padding: 10px 9px;  /* Slightly more padding for headers */
}

td {
    font-size: 12px;  /* Smaller font size */
    color: #333;
}

.add-event-btn {
    margin-top: 10px;
    display: inline-block;
    padding: 6px 12px;  /* Smaller button */
    background-color: #66bb6a;
    color: white;
    border: none;
    border-radius: 5px;
    font-size: 14px;
    text-align: center;
    text-decoration: none;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.add-event-btn:hover {
    background-color: #004d40;
}
</style>
    
</head>
<body>

  <div class="header-buttons">
    <a href="/employee_home" class="btn">Return to Dashboard</a>
</div>
<div class="table-container">
  <h1 style="text-align: center;">All Events</h1>
  <table>
    <thead>
      <tr>
        <th>Event Status</th>
        <th>Event Date & Time</th>
        <th>Location</th>
        <th>Sewing, Non-Sewing, or Both</th>
        <th>Coordinator First Name</th>
        <th>Coordinator Last Name</th>
        <th>Event Duration</th>
        <th>Total People</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      
      <% if (events.length > 0) { %>
        <% events.forEach(event => { %>
          <tr>
            <input type="hidden" value="event.id">
            <td><%= event.event_status == 'P' ? 'Pending' : 'Approved' %></td>
            <td><%= new Date(event.event_date).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' }) + ' @ ' + new Date('1970-01-01T' + event.start_time).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', second: '2-digit' }) %></td>
            <td><%=  event.address + ', ' + event.event_city + ', ' + event.event_state %></td> 
            <td><%= event.sewing_non %></td>
            <td><%= event.coordinator_first_name %></td>
            <td><%= event.coordinator_last_name %></td>
            <td><%= event.event_duration %></td>
            <td><%= event.adult_participants + event.child_participants%></td>
           
            <td>
             
              <form action="/edit_event/<%= event.id %>" method="GET" style="display:inline;">
                <button class="add-event-btn" type="submit">Edit</button>
              </form>
              <form action="/delete_event/<%= event.id %>" method="POST" style="display:inline;">
                <button class="add-event-btn" type="submit" onclick="return confirm('Are you sure you want to delete this event?');">Delete</button>
              </form>
            </td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="7">No data available</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
<form action="/schedule_event"  method="GET">
  <button class="add-event-btn" type="submit">Manually Add an Event</button>
  </form>
</body>
</html>